---
- hosts: raspberrypi_3
#- hosts: macbooks
#- hosts: localhost

#  vars_files:
#    - vars.yml
#  vars:

  roles:
    - ansible-expandfs
#    - ansible-network
#    - ansible-minibian_core
#    - ansible-minibian_lxde        # deps: minibian_core
#    - ansible-vim
#    - ansible-ohmyzsh              # deps: homebrew
#    - ansible-terminus_powerline   # deps: homebrew (fontconfig)
#    - ansible-nerdfonts            # deps: homebrew cask (fontconfig, cask-fonts)
#    - ansible-mpd                  # deps: homebrew
#    - ansible-sshd                 # deps: homebrew
#    - ansible-homebrew             # deps: eliottweiser.osx-command-line-tools
#    - ansible-macos_setup           # deps: homebrew
#    - ansible-terminal              # deps: homebrew; terminus_powerline, nerdfonts
...
