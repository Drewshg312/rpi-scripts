DONE:
=====================

- bootstrap_core (debian)
- mpd (debian, macos)
- ohmyzsh (debian, macos)
- nerdfonts (debian, macos)
- terminus_powerline (debian, macos)
- expandfs (debian, redhat)
- Terminal role (macos)
- PF role (macos)
- ssh role (debian, macos, redhat)
- pihole (non-root, debian)

- change variables to tables in README.md
- add fact gathering to playbook examples in README.md

TODOs:
======================
- dnscrypt (freebsd, macos, redhat)
	- remove unnecessary handlers
	- deal with different arch (maybe choose arch based on fact gathering)

- nerdfonts (fix git clone)

- mpd
	- adopt for centos

- macos_setup (macos)
	- installation of apps from App Store (mas ansible module)

- PF role
	- adopt for freebsd

- network (debian)
	- adopt for redhat
	- adopt for FreeBSD

- vim (debian):
	- vim-airline theme
	- adopt for macos


- KeepassXC:
	- ansible-keepassxc role (centos, debian, macos)

- terminus_powerline
	- add user-specific install


- telegram bot (python)
- docker
	- Start (coreOS, macos)
	- ansible docker
	- elasticsearch
- django/python

- raspberry pi cluster (docker/kubernetes)
	- mesos
	- amsterdam (or at least Elastic Stack)

