DONE:
- mpd (debian, macos)
- ohmyzsh (debian, macos)
- nerdfonts (debian, macos)
- terminus_powerline (debian, macos)


TODOs:

- macos_setup (macos)
	- installation of apps from App Store (mas ansible module)

- Terminal role (macos)
	- REQIREMENTS: homebrew (if terminal_iterm2: yes)
	- iterm switch (yes/no)
	- depends (nerdfonts and terminus_powerline - if iterm: yes)

- SSH role
	- VAULT (encrypted keys and passwords research)

- PF role
	- adapt for macos
	- adapt for freebsd

- Put all ansible roles separately on GitLab and GitHub
	- create nice README for each ansible role
	- add Virustotal devops test to github

- network (debian)
	- adapt for redhat

- expandfs (debian)
	- adapt for redhat


- vim (debian):
	- vim-airline theme
	- adapt for macos


- KeepassXC:
	- ansible-keepassxc role (centos, debian, macos)

- docker
	- Start (coreOS, macos)
	- ansible docker
	- elasticsearch
- django/python

- raspberry pi cluster (docker/kubernetes)
	- mesos
	- amsterdam (or at least Elastic Stack)

